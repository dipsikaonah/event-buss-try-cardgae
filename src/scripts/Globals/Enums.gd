extends RefCounted
class_name Enums

enum GameState { MAIN_MENU, PLAYING, PAUSED, GAME_OVER }
enum Priority { PREV, ADD, MULT}

enum Type {DAMAGE,SHEILD,BUFF}
enum Target { PLAYER, ENEMY, ALL_EN, ALL }
